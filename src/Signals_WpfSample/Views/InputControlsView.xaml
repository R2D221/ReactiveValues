<UserControl
	x:Class="Signals_WpfSample.Views.InputControlsView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:Signals_WpfSample.Views"
	xmlns:common="clr-namespace:ReactiveSamples.Common;assembly=ReactiveSamples.Common"
	mc:Ignorable="d"
	d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
	<UserControl.DataContext>
		<common:InputControlsViewModel/>
	</UserControl.DataContext>
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<GroupBox Header="Input">
			<StackPanel Margin="5">
				<CheckBox Content="Checked" Margin="5" IsChecked="{Binding Checked}"/>
				<StackPanel Orientation="Horizontal">
					<Label Content="Number:" VerticalAlignment="Center"/>
					<TextBox TextWrapping="Wrap" Text="{Binding Number}" Width="120" Margin="5"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Text:" VerticalAlignment="Center"/>
					<TextBox TextWrapping="Wrap" Text="{Binding Text}" Width="120" Margin="5"/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Date:" VerticalAlignment="Center"/>
					<DatePicker SelectedDate="{Binding Date}" Margin="5" />
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Label Content="Item:" VerticalAlignment="Center"/>
					<ComboBox
						Margin="5"
						Width="120"
						ItemsSource="{Binding Items}"
						SelectedItem="{Binding Item}"
						/>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Button Content="Submit" Margin="5" Width="120" Command="{Binding SubmitCommand}"/>
					<Button Content="Clear" Margin="5" Width="120" Command="{Binding ClearCommand}"/>
				</StackPanel>
			</StackPanel>
		</GroupBox>

		<GroupBox Header="Output" Grid.Column="1">
			<StackPanel Margin="5">
				<TextBlock Margin="5">
					<Run Text="Checked: "/>
					<Run Text="{Binding Checked}"/>
				</TextBlock>
				<TextBlock Margin="5">
					<Run Text="Number: "/>
					<Run Text="{Binding Number}"/>
				</TextBlock>
				<TextBlock Margin="5">
					<Run Text="Text: "/>
					<Run Text="{Binding Text}"/>
				</TextBlock>
				<TextBlock Margin="5">
					<Run Text="Date: "/>
					<Run Text="{Binding Date}"/>
				</TextBlock>
				<TextBlock Margin="5">
					<Run Text="Item: "/>
					<Run Text="{Binding Item}"/>
				</TextBlock>
			</StackPanel>
		</GroupBox>

	</Grid>
</UserControl>
